name:                Latte
version:             0.1.0.0
author:              "Piotr Karpiński"

extra-source-files:
- README.md

dependencies:
  - base >= 4.7 && < 5
  - containers >= 0.6.0.1
  - array
  - mtl >= 2.2.2
  - text
  - filepath
  - process
  - text-format
  - lens
  - extra
  - pretty-simple

library:
  source-dirs:
    - src/latte
    - src/parser

executables:
  latc:
    main:                Main.hs
    source-dirs:         src/main
    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N
    dependencies:
    - Latte

